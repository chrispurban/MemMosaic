import {
	__x,
	__o,
} from '../../../tools/defaults';

import {
	atom,
} from "recoil";

/////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////

// track whether a note is currently being edited
export const selectedID_atom = atom({
	key: 'selectedID_atom',
	default: "",
});
